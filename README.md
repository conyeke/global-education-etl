# data_pipeline
This data pipeline automates the extraction, transformation, and storage of data from two sources. It includes functionality for retrieving data from external APIs and saving the resulting data in local JSON files.
